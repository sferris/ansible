all:
  hosts:
    srvoel01:
      ansible_host: srvoel01.wtferris.net
      ansible_user: sferris
      ansible_ssh_private_key_file: /path/to/private/key

  vars:
    ansible_python_interpreter: /usr/bin/python3.12

